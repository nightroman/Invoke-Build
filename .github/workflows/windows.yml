name: windows

on:
  push:
    branches: [ master, test ]
  pull_request:
    branches: [ master ]

jobs:
  test:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2

      - name: test5
        shell: powershell
        run: |
          ./Invoke-Build.ps1 GHA

      - name: test7
        shell: pwsh
        run: |
          ./Invoke-Build.ps1 GHA

      - name: test2
        shell: pwsh
        run: |
          powershell -Version 2 -NoProfile ./Invoke-Build.ps1 GHA
